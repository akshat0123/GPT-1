train:
    description: Train a decoder
    main: train
    sourcecode:
        root: ../
        dest: .
        select: "*.py"
    flags-dest: config:params.yml
    flags-import: all
    requires:
        - file: ../data/model/train.txt
          target-path: data/model
        - file: ../data/model/dev.txt
          target-path: data/model
